# TODO — Tablero operativo

## NOW
- [x] Fase 1 — Foundation: elegir stack concreto y dejar la home pública del atlas ejecutándose como aplicación Next.js con App Router.

## NEXT
- [x] Definir proveedor de autenticación y política de persistencia para usuarios registrados, conforme a la arquitectura recomendada en `MASTER_PACKAGE/08_ARCHITECTURE.md` y `09_SECURITY.md`.
- [x] Configurar el contrato de almacenamiento privado para fotografías de cultivo y buckets públicos para media editorial, acorde a `MASTER_PACKAGE/06_VISUAL.md` y `09_SECURITY.md`.
- [x] Preparar el primer esquema de datos público en Postgres/compatible y la capa de servicio Open-Meteo proxy con caché regional de la Fase 5.
- [x] Implementar la estructura geográfica verificada del modelo público: `Argentina -> provincia -> departamento/partido -> zona editorial aproximada`, sin clasificar ni regionalizar clima. El endpoint `GET /api/geo` deja la jerarquía y el tipo de unidades como contrato de entidad, separando la estructura del dato climático todavía `PARTIAL_RESEARCH`.
- [x] Implementar la primera capa funcional de selección geográfica basada en provincia/partido/departamento/zona aproximada en la UI pública, como flujo `Argentina → provincia → departamento/partido (opcional) → zona aproximada (opcional)`, con exploración libre sin ubicación, usando el endpoint `GET /api/geo` y los contratos existentes. Archivos afectados: `src/app/page.js`, `src/app/components/GeoSelector.js`, `src/app/globals.css`. Verificaciones: smoke test del endpoint `/api/geo` y smoke test de la home con `curl`; `npm run build` exit 0; `get_errors` sin errores en los archivos relevantes.
- [x] Auditoría de arquitectura y UX sobre la estructura geográfica ya implementada: `province_id`, `department_id` y `zone_id` quedan como referencias de ubicación geográfica, desacopladas de cualquier dato climático o capa meteorológica. La estructura `ARGENTINA → PROVINCIA → DEPARTAMENTO/PARTIDO → ZONA_APROXIMADA` es válida para un flujo futuro `UBICACIÓN SELECCIONADA → CONTEXTO METEOROLÓGICO → WIDGET CLIMÁTICO → INTERPRETACIÓN EDITORIAL → CONTENIDO DEL ATLAS` si el clima later se modela como un componente independiente y reutilizable con `selected_location` como referencia de ubicación aproximada (`province_id`/`department_id`/`zone_id`) y sin guardar lat/lng ni dirección. El mapa sigue siendo la dimensión geográfica; el clima queda en el futuro como widget/panel ambiental, nunca sobre el mapa ni mezclado con el estado editorial de la zona. No se encontró acoplamiento que obligue a rehacer `/api/geo`. La capa `zona aproximada` queda tratada como capa UX/editorial, no como clasificación científica, y la documentación climática (`MASTER_PACKAGE/04_CLIMATE.md`) y el contrato de geo (`src/app/api/geo/route.js`) ya separan la fuente climática del modelo geográfico. El soporte actual para un futuro widget climático es de tipo `prepared` por desacoplamiento arquitectónico, y no requiere ninguna nueva decisión de infraestructura ni de proveedor para esta auditoría. Próxima tarea concreta: dejar abierta la actividad editorial de contenido con la capa de `zona aprox.` sólo como agrupación editorial y nunca como clase climática; no continuar automáticamente con esa tarea fuera de esta auditoría.
- [x] Implementar la Home visual y funcional según la especificación de `MASTER_PACKAGE/22_HOME_EXPERIENCE.md`, usando `GeoSelector`, `EnvironmentalPanel` con estado `NO_IMPLEMENTADO`, `CategoryShowcase` y `HomeSecondaryAccess` en una composición de componentes sin crear ni combinar capa de clima ni regionalización, verificando el endpoint `/api/geo` y el árbol de la home con `npm run build` y `get_errors` en los archivos relevantes. No se implementan proveedores ni datos meteorológicos y no se crea ninguna agrupación editorial de zona nueva.

## BLOCKED
- [x] Proveedor de auth/base de datos: **resuelto en Fase 10B** — Supabase (Postgres + Auth), ver `MASTER_PACKAGE/40_PHASE_10B_ACCOUNT_AUTH.md`. Usado hoy solo para "Mi Cultivo" (registro/login/logout reales, tablas `cultivos`/`cultivo_events` con RLS).
- [ ] Proveedores de email/newsletter y hosting siguen sin decisión cerrada. El proyecto no debe inventar estos servicios sin decisiones de infraestructura.
- [ ] Investigación editorial y geográfica pendiente en `MASTER_PACKAGE/19_SOURCE_REGISTRY.md` sobre regionalización y legal/regulación.
- [ ] No hay una tarea de implementación VSC independiente sin depender del gate `PARTIAL_RESEARCH` geográfico y agroclimático documentado en `MASTER_PACKAGE/21_GEO_CLIMATE_RESEARCH.md` y en el cierre de la investigación de `CLAUDE_RESEARCH_REQUIRED`.

## CLAUDE_RESEARCH_REQUIRED → PARTIAL_RESEARCH (actualizado 2026-09-09, ver `MASTER_PACKAGE/21_GEO_CLIMATE_RESEARCH.md`)
- [x] Clasificación climática oficial citable: **resuelto parcialmente**. Se verificó por lectura directa una capa Köppen oficial (INDEC/ANIDA-IGN, `geonode.indec.gob.ar/layers/geonode_data:geonode:tipos_climaticos`, VERIFIED) utilizable como referencia editorial — ver decisión D10 en `MASTER_PACKAGE/16_DECISIONS.md`. Licencia figura "Not Specified": **no redistribuir el archivo de la capa** hasta gestionarla con INDEC/IGN.
- [ ] Regionalización agroecológica reutilizable (INTA RIAN / "regiones agroeconómicas homogéneas" RySA): **sigue sin verificación primaria**.
  - Información faltante: `agroregionesrian.inta.gob.ar`, `climayagua.inta.gob.ar`, `rian.inta.gov.ar` y `geoportal.idesa.gob.ar` no son accesibles desde este entorno de ejecución (fallo de DNS/conexión, reproducible en dos rondas de investigación distintas) — no es evidencia de que no existan (la indexación de buscador confirma que sí existen y da forma aproximada de su contenido), pero impide leerlos de primera mano.
  - Atlas Climático SMN (129 pág.) y Estadísticas Climatológicas Normales: existen y están confirmados, pero exceden el límite de tamaño (10MB) de las herramientas de fetch de esta sesión — contenido interno (¿usa Köppen explícito?, datos por estación) sigue `UNVERIFIED`.
  - Heladas por deciles de INTA: metodología y un ejemplo numérico (Balcarce) corroborados solo por snippet de búsqueda, no lectura primaria. Se identificó una vía alternativa prometedora no gubernamental — `heladas.agro.uba.ar` — que no fue probada en esta ronda por alcance, y es el paso de mayor prioridad para la próxima.
  - Por qué sigue siendo necesaria: el agrupamiento editorial de departamentos en "zonas" (`03_GEO.md`, decisión D1) puede acelerarse si INTA RIAN o RySA resultan reutilizables, pero eso no puede confirmarse sin leer esas fuentes directamente — usarlas hoy sin leerlas sería inventar contenido, algo explícitamente prohibido.
  - Próximos pasos concretos (en orden de impacto): (1) reintentar el acceso a los 4 dominios bloqueados desde una red sin esta restricción; (2) probar `heladas.agro.uba.ar` como vía alternativa a los datos de heladas de INTA; (3) descargar y releer el Atlas Climático SMN completo fuera de esta sesión; (4) confirmar si "regiones agroeconómicas homogéneas" (RySA) cubre todo el país o solo NOA, y su licencia.
  - Qué parte del proyecto depende: Fase 2 geográfica, el catálogo de `zones`, y cuánto trabajo editorial manual de agrupamiento hace falta (podría reducirse sustancialmente si estas fuentes resultan reutilizables, pero no se puede asumir eso todavía).

- [ ] Resolver la investigación de proveedores de infraestructura aún no cerrada: auth, email/newsletter, hosting y el modelo de terceros de anuncios/correos transaccionales. 
  - Información faltante: falta decidir si se usará una auténtica implementación de Auth.js con CredentialsProvider o con un provider gestionado; proyecto no debe elegir un servicio de email/newsletter o hosting si no hay un contrato de decisiones y un proveedor identificado.
  - Pregunta concreta: ¿cuál es el proveedor de autentificación y persistencia del usuario, cuál es el proveedor de email/newsletter, y cuál es el proveedor de hosting? ¿Hay un proveedor de dominio y registrar, un SMTP concreto, y una política de sandbox/deploy real en el entorno?
  - Fuentes a consultar: `MASTER_PACKAGE/16_DECISIONS.md`, `MASTER_PACKAGE/18_EXTERNAL_SERVICES.md`, `MASTER_PACKAGE/09_SECURITY.md`, `MASTER_PACKAGE/08_ARCHITECTURE.md`, y el registrar de servicio de decision/cost en `MASTER_PACKAGE/19_SOURCE_REGISTRY.md`.
  - Por qué es necesaria: la implementación de rutas con `Auth.js`, `API routes`, `storage` y `climate` solo se puede cerrar con una elección técnica de proveedor de infraestructura; no se debe inventar una implementación hosted, email transaccional/newsletter, o auth sin definir esa decisión.
  - Qué parte del proyecto depende: la capa de acceso, la política de producción, la entrega de emails de recuperación, password reset, newsletter, alertas y el hosting final de la app.

## DONE (Loop 1 — Crecimiento / Germinación, base editorial)
- [x] Loop 1: documentado en `MASTER_PACKAGE/48_LOOP_1_GERMINACION_BASE.md`. Se completó la entrada `germinacion` con 4 secciones nuevas (Historia —un párrafo, distingue estimación genómica de domesticación de *Cannabis sativa* [~12.000 años BP, Ren et al. 2021, Science Advances] de evidencia arqueobotánica fechada de semilla/fitolito [Haimenkou ~1650-400 a.C., Dal Martello et al. 2023; Shandong 4.500-3.400 BP, Liu et al. 2026]—; estado/viabilidad de la semilla antes de germinar, vía INASE; una síntesis explícita "qué ocurre / qué se ve / por qué" separando proceso de observación; y relación con otras etapas ya publicadas del Atlas) más el modelo trifásico de germinación reforzado con Bewley (1997, The Plant Cell, revisión clásica verificada por lectura directa). Se corrigió una fuente ya existente que tenía autoría/revista `UNVERIFIED` desde la Fase 7A (Langa et al. 2024, confirmado vía Crossref como *South African Journal of Botany*). Ningún parámetro numérico de germinación se presentó como regla universal; ninguna fecha de "primer cultivo" se afirmó sin evidencia. No se tocó la contextualización provincial (Fase 47.2), Mi Cultivo, Chatbot, Supabase ni el diseño del Atlas — verificado con la suite `tests/provincial-context-audit.mjs` sin modificar: 1280/1280 en verde. `npm run build` limpio, mismas 9 rutas. No se continuó con las otras 6 entradas (loops separados).

## CLAUDE_REVIEW_REQUIRED
- [ ] Si se requiere una decisión relevante de seguridad, permisos, privacidad o contenido legal en la integración del backend/seguridad/Pii del usuario, la matriz de tareas exige revisión humana de Claude.

## DONE (Patch 47.1 — Home y contextualización geográfica)
- [x] Patch 47.1: documentado en `MASTER_PACKAGE/47_1_HOME_AND_GEO_CONTEXT_PATCH.md`. Home simplificado: se quitó el CTA "Chatbot del Atlas" (el chatbot sigue intacto en `/chatbot`, solo dejó de promocionarse desde la entrada), "Mi Cultivo" → "Ingresar", "Ingresar libremente" → "Explorar el Atlas" (mismos destinos). "¿Dónde cultivás?" ahora tiene una oración breve y **la selección de provincia dejó de ser decorativa**: se encontró que el botón "Explorar {provincia}" de `GeoSelector` no tenía ningún `onClick` (no hacía nada) y que ya existía en el código una convención de `localStorage` (`atlas:selectedProvince`/`atlas:selectedZone`) leída por un componente huérfano (`EnvironmentalPanel.js`, nunca importado en ninguna página) pero que `GeoSelector` nunca escribía — se conectó ese circuito: ahora la elección se persiste (mismo id de provincia que usa `lib/weather/locations.js` desde la Fase 11) y se hidrata al volver a Home. No se conectó ningún dato climático real a ninguna página del Atlas ni se revivió `EnvironmentalPanel.js` — eso queda fuera de este patch. `npm run build` limpio (9 rutas, sin cambios). Regresión y responsive (1440×900/1280×800/768×1024/390×844) confirmados sin overflow ni errores de consola.

## PENDIENTE — detectado durante el Patch 47.1 (no iniciar sin instrucción explícita)
- [ ] `src/app/components/EnvironmentalPanel.js` sigue siendo un componente huérfano (no se importa en ninguna página). Ahora que `GeoSelector` sí alimenta las claves de `localStorage` que espera, es un candidato concreto para una fase futura que reutilice `fetchProvinceWeather` (Fase 11) y muestre clima real — pero dónde mostrarlo sin convertir Home en un dashboard es una decisión de producto pendiente, no resuelta acá.

## DONE (Patch 47.2 — Contextualización provincial del Atlas)
- [x] Patch 47.2: documentado en `MASTER_PACKAGE/47_2_PROVINCIAL_CONTEXT.md`. Cada una de las 7 entradas del Atlas ahora incorpora una sección "Contexto de tu zona" (`ProvinceContextPanel.js`) que lee la misma provincia ya persistida por `GeoSelector` (`atlas:selectedProvince`, sin claves ni estado nuevo) y arma 1-2 párrafos con macrorregión (NOA/NEA/Cuyo/Pampeana/Patagonia, agrupación geográfica de uso corriente, no climática) y, en Luz y fotoperiodo/Cosecha y maduración, la latitud real de la provincia (`lib/weather/locations.js`, Fase 11) — sin inventar ningún dato de temperatura, lluvia, humedad, helada u hora de luz. `marco-editorial` no varía por provincia (sin fuente legal provincial verificada) y muestra siempre la misma nota de alcance nacional. Sin provincia elegida, muestra "Elegí tu provincia para contextualizar esta información." en vez de inventar contexto. También se corrigió la denominación visible "Fundamentos" → "Crecimiento" (grilla, tarjeta, breadcrumb; slug técnico `fundamentos` intacto). Prueba nueva `tests/provincial-context-audit.mjs` (Playwright directo, sin test runner) cubrió las 24 jurisdicciones × 7 entradas (persistencia, nombre correcto, sin fuga de otra provincia, sin `undefined`/`null`, sin imágenes rotas, sin overflow, sin errores de consola), coherencia por macrorregión, diferenciación real por latitud, aislamiento explícito (Buenos Aires→Mendoza→Tierra del Fuego→CABA), "Explorar sin elegir" y responsive en 4 resoluciones: **1280/1280 verificaciones en verde**. `npm run build` limpio, mismas 9 rutas. No se tocó Mi Cultivo, Chatbot, Supabase/Auth/RLS/Storage, ni ninguna clasificación climática (sigue `PARTIAL_RESEARCH`). No se iniciaron los 8 loops de investigación editorial.

## DONE (Cierre funcional del producto)
- [x] Cierre funcional del Atlas antes de retomar investigación: documentado en `MASTER_PACKAGE/47_PHASE_PRODUCT_COMPLETION.md`. Auditoría en vivo (Playwright) de Home, Atlas (8 tarjetas: 7 categorías + "De semilla al frasco"), las 7 categorías, las 7 entradas editoriales, Mi Cultivo (sin cuenta y con cuenta: evento, edición, provincia, clima real, foto real, Mi Temporada), Chatbot (retrieval + contexto, con y sin sesión), Auth (signup/login/logout/sesión persistente entre Mi Cultivo y Chatbot), Créditos, `/api/geo`, `/api/climate` — todo funcionando, sin links rotos, sin errores de consola, sin overflow en 4 resoluciones (1440×900, 1280×800, 768×1024, 390×844). Se encontró y corrigió una inconsistencia real de documentación de infraestructura: el endpoint `src/app/api/storage/route.js` describía un contrato de Cloudflare R2 nunca implementado (confirmado sin ningún consumidor real vía `grep`) — se eliminó (build pasó de 10 a 9 rutas), y se marcó la Decisión D9 como superada por la implementación real de Supabase Storage de la Fase 10C, con notas de actualización en `18_EXTERNAL_SERVICES.md` y `DECISIONS.md`. Se confirmó que el modelo de entradas editoriales (`sections[]` genérico) ya soporta agregar "Historia breve"/"Cómo se hace" sin ningún cambio de código cuando se retome la investigación. `npm run build` limpio, `get_advisors` sin alertas. La investigación de los 8 loops editoriales (Germinación, Sustrato y drenaje, Luz y fotoperiodo, Lectura de señales, Cultivo en secuencia, Cosecha y maduración, Marco argentino, Síntesis general) queda explícitamente **pausada**, sin pérdida de ningún trabajo previo (el intento de mapa de conocimiento de Germinación no había llegado a crear ningún archivo), lista para retomarse en la próxima fase.

## DONE (Fase 13C)
- [x] Fase 13C — Chatbot del Atlas: contexto personalizado: documentado en `MASTER_PACKAGE/46_PHASE_13C_CHATBOT_CONTEXT.md`. Nueva capa `src/app/lib/chatbot/context.js` (`loadUserContext`) que arma, en cada búsqueda, un paquete de contexto autorizado del usuario autenticado además del retrieval editorial de 13B: etapa/fechas/notas de sus propios eventos de Mi Cultivo (sin UUIDs internos), cantidad de fotos (nunca las fotos en sí — nueva función liviana `countPhotosByEvent` en `photos.js`, sin generar URLs firmadas innecesarias), provincia guardada y clima actual de esa provincia (reutilizando sin cambios `fetchProvinceWeather`/`fetchCultivo`/`stageLabel` ya existentes de las Fases 10B/11/12). El contexto siempre se arma a partir de `session.user.id` real, nunca de un id externo; el aislamiento entre cuentas lo sigue haciendo RLS sin ninguna política nueva ni modificada — verificado contra Postgres real con dos cuentas (`set local request.jwt.claims`: 0 filas visibles entre sí, 1 fila propia). La UI solo muestra dos indicadores discretos ("Contexto de Mi Cultivo disponible" / "Contexto ambiental disponible"), nunca datos crudos ni IDs. Ningún dato inventado: sin provincia o con el proveedor caído, `climate.available: false` con fallback limpio, igual que en Mi Cultivo. Ningún campo de diagnóstico o conclusión — fotos/clima/etapa/eventos quedan como contexto crudo, nunca como interpretación. `npm run build` limpio (10 rutas, sin cambios). Regresión de Home/Atlas/categoría/entrada/Mi Cultivo/Auth/Créditos/`/api/geo`/`/api/climate` confirmada, responsive 1440×900/390×844 sin overflow. `get_advisors`: sin alertas. No hay LLM, Internet, memoria, historial ni modificación de Mi Cultivo. Quedaron 2 archivos de prueba huérfanos en el bucket privado (ver documentación, §13 — inaccesibles, sin riesgo real; lección de proceso para limpieza de pruebas futuras). Siguiente fase sugerida: 13D (motor de respuesta) — no iniciada.

## DONE (Fase 13B)
- [x] Fase 13B — Chatbot del Atlas: retrieval del conocimiento editorial: documentado en `MASTER_PACKAGE/45_PHASE_13B_CHATBOT_RETRIEVAL.md`. Nueva capa `src/app/lib/chatbot/searchAtlas.js` — búsqueda local, determinista y sin dependencias sobre el modelo editorial ya existente (`lib/editorial/registry.js`/`editorialData.js`/`sources.js`, reutilizados sin cambios ni duplicación). Normalización simple (minúsculas, sin acentos, sin signos) + coincidencia por prefijo de hasta 6 caracteres (cubre "semilla"/"semillas" y "germinar"/"germinación" sin diccionario de sinónimos). Scoring por campo con pesos (título 10, resumen 9, tags 8, título de sección 6, contenido de sección 4, observaciones 3, señales 2, errores frecuentes 2, contexto ambiental 1) y umbral mínimo de 3 para filtrar coincidencias débiles; máximo 5 resultados, nunca rellenados artificialmente. Cada resultado conserva `entryId`/`categoryId`/`sectionId`/`sourceIds`/`url` reales — trazabilidad completa hasta la entrada, sección y fuente. Snippets son siempre extracción literal del texto editorial real, nunca parafraseados. `/chatbot` (protegido desde 13A) ahora ejecuta búsquedas reales: 7 consultas de prueba verificadas manualmente contra el contenido real (germinación, cómo germina una semilla, luz y fotoperiodo, suelo y drenaje, cosecha, errores durante la germinación, y una consulta sin evidencia que correctamente devolvió `[]`), con verificación explícita de que los resultados secundarios de menor puntaje son menciones reales del término en el texto editorial (no ruido). Sin sesión, el formulario de búsqueda no existe en el DOM — el retrieval nunca se ejecuta sin autenticación. No se usó LLM, Internet, clima ni Mi Cultivo; no hay memoria ni historial de conversaciones; no se instaló ninguna dependencia nueva; no se creó ninguna tabla Supabase. `npm run build` limpio (10 rutas, sin cambios). Regresión de Home/Atlas/categoría/entrada/Mi Cultivo/Auth/Créditos/`/api/geo`/`/api/climate` confirmada, responsive 1440×900/390×844 sin overflow. `get_advisors`: sin alertas. Siguiente fase sugerida: 13C (contexto autorizado del usuario) — no iniciada.

## DONE (Fase 13A)
- [x] Fase 13A — Chatbot del Atlas: acceso autenticado + estructura base: documentado en `MASTER_PACKAGE/44_PHASE_13A_CHATBOT_AUTH.md`. Nueva ruta `/chatbot`, exclusiva para usuarios autenticados, reutilizando exactamente el mismo patrón de auth de cliente ya usado en Mi Cultivo (`getSupabaseClient` + `getSession`/`onAuthStateChange`) — no se introdujo middleware, SSR ni una segunda arquitectura de autenticación. Sin sesión: pantalla de acceso ("Esta función está disponible para usuarios con cuenta") con dos CTAs reales hacia `/mi-cultivo` (mismo formulario real de la Fase 10B, sin login paralelo ni cuentas demo); la interfaz de chat no se renderiza en el DOM en absoluto sin sesión (rama condicional, no CSS). Con sesión: interfaz base real (estado inicial, input usable, botón "Enviar" permanentemente deshabilitado, nota editorial "en desarrollo") — sin motor de respuesta, sin LLM, sin retrieval, sin llamadas a ningún API externo ni a editorialData/Mi Cultivo/clima. Botón nuevo "Chatbot del Atlas" agregado al hero de la Home junto a los dos ya existentes, mismo estilo, sin rehacer navegación. Probado con una cuenta real (signup → login → acceso → logout → bloqueo), responsive 1440×900/390×844 sin overflow, sin errores de consola, `npm run build` limpio (10 rutas). Regresión de Home/Atlas/categoría/entrada/Mi Cultivo/Auth/Créditos/`/api/geo`/`/api/climate` confirmada. `get_advisors`: sin alertas (no se tocó ninguna política ni tabla). No se instaló ninguna dependencia nueva. Siguiente fase sugerida: 13B (retrieval del Atlas) — no iniciada.

## DONE (Fase 12)
- [x] Fase 12 — Mi Temporada: historial temporal de Mi Cultivo: documentado en `MASTER_PACKAGE/43_PHASE_12_MI_TEMPORADA.md`. Nueva sección "Mi temporada" (inicio real, tiempo transcurrido, etapa actual, cantidad de eventos y, con cuenta, cantidad de fotos) más "Línea temporal" (antes "Eventos registrados", ahora con orden reciente/cronológico) y "Próxima etapa" (solo navegación, sin fechas inventadas), todo dentro de la misma página `/mi-cultivo`, sin ruta nueva. Un solo concepto de "inicio de temporada": la fecha del primer evento real (nunca `createdAt` del registro ni la fecha de hoy) — sin eventos, estado vacío claro. La sección de clima de la Fase 11 se reetiquetó como "Hoy" sin duplicar nada: ya mostraba etapa actual + contexto ambiental + pronóstico + alertas. No se implementó clima histórico por evento (ningún evento tiene captura ambiental real en el momento en que ocurrió) ni calendario mensual (la línea temporal con modo cronológico alcanza para el volumen real de datos). Se agregó **eliminar un evento individual** (antes solo existía el reinicio total): confirmación de dos toques, en cuenta usa una `deleteEventRemote` nueva protegida por la misma política RLS ya vigente (sin políticas nuevas, `get_advisors` en 0), y borra primero las fotos del evento con la función ya existente de la Fase 10C (verificado: 0 objetos huérfanos en Storage tras borrar un evento con foto). Aislamiento entre cuentas reverificado en vivo con control positivo y negativo contra Postgres real (usuario B no puede borrar eventos de A; A sí puede borrar los propios). Ningún esquema ni bucket nuevo — todo se calcula en el cliente a partir de datos que ya existían. `npm run build` limpio (mismas 9 rutas). Regresión de Home/Atlas/Créditos/`/api/geo`/`/api/climate` confirmada, sin overflow ni errores de consola en 1440×900/390×844, incluido un caso de 8 eventos cargados. No se tocó Home, mapa, Atlas, Auth ni Storage salvo la reutilización sin cambios ya descripta; no se implementó chatbot, notificaciones, histórico meteorológico ni calendario agronómico prescriptivo.

## DONE (Fase 11)
- [x] Fase 11 — Clima personalizado + contexto ambiental de Mi Cultivo: documentado en `MASTER_PACKAGE/42_PHASE_11_CLIMA_MI_CULTIVO.md`. Nueva sección "Contexto ambiental" en Mi Cultivo (sin cuenta y con cuenta): selector de provincia aproximada + temperatura/humedad/lluvia/viento actuales, pronóstico corto, lecturas "qué está pasando hoy" y alertas por umbral (helada, lluvia abundante) calculadas sobre el pronóstico real. Fuente: **Open-Meteo**, reconfirmando la Decisión D2 ya vigente (releído en vivo: sigue sin requerir API key para uso no comercial, cobertura y límites sin cambios) — no se creó ninguna decisión nueva. SMN e INTA siguen sin API consumible (SMN devuelve HTTP 402, INTA sigue con DNS inaccesible), se los deja solo como referencia de autoridad enlazada. Cero infraestructura de servidor nueva: se reutilizó el proxy `/api/climate` que ya existía de una fase anterior. Coordenadas representativas por provincia obtenidas del centroide oficial de georef-ar-api (misma fuente ya usada en la Fase 8A para el mapa), con una excepción documentada para Tierra del Fuego (se usa el centroide de Ushuaia en vez del de la provincia, que cae en la Antártida). Solo persiste en Supabase la provincia elegida (`cultivos.province_id`, columna nueva, sin políticas RLS nuevas necesarias — confirmado con `get_advisors`). Aislamiento entre usuarios verificado contra Postgres real con dos cuentas reales: un usuario no puede leer ni modificar la ubicación de otro. Se encontró y corrigió una condición de carrera real (misma clase de bug que el de fotos en la Fase 10C): elegir provincia antes de que el cultivo remoto terminara de cargar podía perder la selección en silencio — corregido deshabilitando el selector hasta que el cultivo remoto está listo. Fallback verificado (proveedor caído → "Datos ambientales no disponibles en este momento", nunca ceros falsos). `npm run build` limpio (mismas 9 rutas). Regresión de Home/Atlas/Créditos/`/api/geo`/`/api/climate` confirmada, sin overflow ni errores de consola en 1440×900/390×844. No se creó ninguna Edge Function, no se implementaron notificaciones ni datos históricos, no se tocó el mapa, el Atlas, la Home ni el modelo de Auth.

## PENDIENTE — detectado durante Fase 11 (no iniciar sin instrucción explícita)
- [ ] Si en el futuro se necesitan notificaciones sobre alertas (push/email/Telegram/WhatsApp), es una fase nueva — explícitamente fuera de alcance de la Fase 11.
- [ ] Reintentar el acceso a los dominios de INTA RIAN y a la página de avisos del SMN (`avisos_a_muy_corto_plazo`, HTTP 402 persistente) desde un entorno sin esa restricción, para evaluar si en algún momento pueden reemplazar o complementar el cálculo de alertas por umbral con una fuente oficial real.
- [ ] Datos históricos/series de tiempo climático: no evaluados en esta fase por alcance — evaluar en una fase futura si se justifica la complejidad.

## DONE (Fase 10C)
- [x] Fase 10C — Mi Cultivo: fotos reales con Supabase Storage: documentado en `MASTER_PACKAGE/41_PHASE_10C_MI_CULTIVO_PHOTOS.md`. Cada evento de "Mi Cultivo" en modo con cuenta ahora puede tener fotos reales: subir, ver ampliada, eliminar. Bucket privado `cultivo-photos` (nunca público), path `{user_id}/{cultivo_id}/{event_id}/{uuid}.jpg`, tabla nueva `cultivo_event_photos` con RLS (`auth.uid() = user_id`) y políticas equivalentes sobre `storage.objects` (aislamiento por primer segmento del path). Procesamiento de imagen 100% en el navegador vía `<canvas>` (sin dependencias nuevas): redimensiona a 1600px máx. y reexporta como JPEG, lo que de paso elimina metadatos EXIF/GPS tal como exige `10_PRIVACY.md`. El modo sin cuenta no cambia: sigue sin fotos, mismo cartel de siempre — limitación documentada, no un olvido. Se encontró y corrigió un bug real durante las pruebas (el efecto de carga de fotos podía dispararse con el id de cultivo local en vez del remoto, justo durante la migración). Bloqueo real encontrado y resuelto con autorización explícita: el límite de envío de emails de Supabase seguía activo y esta fase prohibía recrear la Edge Function de prueba de la Fase 10B — se resolvió registrando una cuenta real por la app y confirmando el email por SQL directo (sin fabricar usuarios), no con atajos de código. Aislamiento entre usuarios verificado con **dos cuentas reales** actuando explícitamente como cada una contra el motor de Postgres real: cero filas visibles entre sí, intento de borrado sin efecto confirmado, cero objetos de Storage visibles. Esa misma verificación encontró una **brecha de seguridad real**: la política de `insert` de `cultivo_event_photos` solo exigía `auth.uid() = user_id`, sin validar que el `cultivo_id`/`event_id` de la fila realmente pertenecieran a ese usuario — permitía insertar una fila propia "colgada" del cultivo/evento de otra cuenta (sin exponer lectura, porque `select` sí sigue exigiendo `user_id` propio, pero sí una brecha de integridad referencial). Corregida antes de cerrar la fase (migración `tighten_cultivo_event_photos_insert_policy`, agrega `EXISTS` contra `cultivos`/`cultivo_events`) y reverificada: el mismo ataque ahora se rechaza, la subida legítima sigue funcionando igual. `get_advisors` de seguridad: sin alertas (antes y después del fix). `npm run build` limpio (mismas 9 rutas). Regresión de Home/Atlas/Créditos/API confirmada. No se creó ninguna Edge Function nueva, no se recreó `temp-create-test-user`, no se tocó el mapa, el Atlas, Home ni el modelo de auth de 10B.

## PENDIENTE — detectado durante Fase 10C (no iniciar sin instrucción explícita)
- [x] Repetir la prueba de aislamiento con una segunda cuenta real — hecho durante la misma fase (ver arriba), no quedó pendiente.
- [ ] Evaluar configurar un proveedor SMTP propio en Supabase (mismo pendiente ya anotado en Fase 10B) — ahora también relevante para poder probar con múltiples cuentas reales sin fricción.
- [ ] El bucket `cultivo-photos` y la tabla `cultivo_event_photos` quedan sin uso real en producción hasta que haya usuarios reales — sin acción pendiente, solo queda anotado que ya están listos.

## DONE (Fase 10B)
- [x] Fase 10B — Cuenta real + Mi Cultivo asociado: documentado en `MASTER_PACKAGE/40_PHASE_10B_ACCOUNT_AUTH.md`. Se detuvo el loop antes de escribir código porque el auth existente (`/api/auth/[...nextauth]`) resultó ser completamente falso (usuario hardcodeado en el código fuente, sin base de datos) — se reportó el bloqueo de infraestructura ya conocido en vez de improvisar. El usuario resolvió conectando un proyecto real de Supabase vía MCP. Se implementó autenticación real (Supabase Auth: registro, login, logout, sesión) reemplazando por completo el NextAuth falso (`next-auth` desinstalado, ruta eliminada). Nuevas tablas Postgres `cultivos`/`cultivo_events` con Row Level Security real (`auth.uid() = user_id` en las 4 operaciones de ambas tablas) — verificado con dos cuentas reales que un usuario no puede ver el cultivo de otro. Capa de acceso `remoteStorage.js` (misma forma que `storage.js` de Fase 10A) para que la UI no distinga local de remoto. Mi Cultivo ahora funciona en dos modos reales: sin cuenta (local, sin cambios de 10A) y con cuenta (asociado al usuario, persistente en Postgres). Migración local → cuenta implementada con oferta explícita y manejo de conflicto (cultivo local + cultivo de cuenta ya existente) sin pérdida de datos en ningún caso. `.env.local` + `.gitignore` nuevos (sin secretos en el código). `npm run build` limpio (9 rutas, una menos que antes). Probado extremo a extremo contra la base real con Playwright en 1440×900/390×844: creación de cuenta, login, logout, migración limpia, migración con conflicto, edición sin duplicar, aislamiento real entre dos usuarios, sin overflow, sin errores de consola. `get_advisors` de seguridad de Supabase: sin alertas. No se implementaron fotos, chatbot, alertas ni clima personalizado; no se tocó el mapa ni el grid del Atlas.

## PENDIENTE — detectado durante Fase 10B (no iniciar sin instrucción explícita)
- [ ] Borrar manualmente la Edge Function `temp-create-test-user` del proyecto Supabase (dashboard → Edge Functions) — se usó para crear 2 cuentas de prueba confirmadas sin gastar el límite de envío de emails, y se neutralizó (devuelve 410, exige JWT) pero no se pudo eliminar vía las herramientas disponibles.
- [ ] Evaluar configurar un proveedor SMTP propio en Supabase antes de un lanzamiento real — el límite gratuito de envío de emails de confirmación de cuenta es muy bajo (pensado para desarrollo, no para usuarios reales en volumen).
- [ ] Carga real de fotos para eventos de Mi Cultivo — sigue sin implementar en ambos modos; la estructura de `cultivo_events` ya admite agregar una columna/tabla de fotos sin migrar lo existente (ver `40_PHASE_10B_ACCOUNT_AUTH.md` §9).

## DONE (Fase 10A)
- [x] Fase 10A — Persistencia de Mi Cultivo: documentado en `MASTER_PACKAGE/39_PHASE_10A_MI_CULTIVO_PERSISTENCE.md`. El historial de Mi Cultivo (Fase 9) ahora sobrevive a cerrar/recargar la página, usando `localStorage` — el mismo mecanismo ya establecido en el proyecto para persistencia sin cuenta (`atlas:selectedProvince`/`selectedZone` en `EnvironmentalPanel`, documentado en `01_PRODUCT.md`/`02_UX.md`). Nueva capa de acceso `src/app/lib/miCultivo/storage.js` (`loadCultivo`/`saveCultivo`/`resetCultivo`) que la UI usa exclusivamente — nunca toca `localStorage` directamente — para poder reemplazarla por un backend autenticado más adelante sin rehacer la interfaz. Modelo extendido (`model.js`): `Cultivo {id, currentStageId, events, createdAt, updatedAt}`, eventos con `createdAt`/`updatedAt` además de los campos ya existentes. Se agregó edición de eventos existentes y un botón de reinicio con confirmación en dos pasos. La tarjeta "Sin cuenta" de la UI se reescribió para decir la verdad ahora que hay persistencia: se guarda en este navegador, sin cuenta ni sincronización entre dispositivos. `npm run build` limpio. Playwright en 1440×900/390×844 confirmó: creación de evento persiste en `localStorage`, sobrevive a un reload sin duplicarse, edición actualiza sin duplicar, avance de etapa persiste, reinicio limpia todo, responsive sin overflow, sin errores de consola. Regresión de Home/Atlas/API confirmada. No se implementó login, base de datos real, upload de fotos, chatbot, alertas ni clima personalizado; no se tocó el mapa ni el Atlas.

## DONE (Fase 9)
- [x] Fase 9 — Mi Cultivo, primera versión funcional: documentado en `MASTER_PACKAGE/38_PHASE_9_MI_CULTIVO_FUNCTIONAL.md`. `/mi-cultivo` pasó de página explicativa estática a una experiencia real de historial: timeline interactiva de 7 etapas (Semilla → Germinación → Trasplante/tierra → Crecimiento → Floración → Cosecha → Maduración/almacenamiento), formulario para registrar eventos (etapa, fecha, nota, foto reservada pero deshabilitada) y lista de eventos registrados en la visita — todo en estado de React (`useState`), sin `localStorage` ni backend, para que "sin cuenta = sin persistencia" sea literalmente cierto y no una simplificación. Modelo de datos nuevo en `src/app/lib/miCultivo/model.js` (`STAGES`, `createSessionEvent`). Dos tarjetas de modo claramente diferenciadas ("Sin cuenta", activa, con advertencia explícita de que los datos se pierden al salir; "Con cuenta", marcada "Próximamente", sin login falso). Se corrigió el botón de Home "Ingresar como usuario" (prometía un login inexistente) a "Mi Cultivo". `npm run build` limpio. Playwright en 1440×900/390×844: timeline funcional (marcar etapa actual, registrar evento con datos correctos, avance automático de etapa), responsive sin overflow, sin errores de consola. Regresión de `/api/geo`, `/api/climate`, `/atlas`, `/creditos` confirmada. No se implementó login real, base de datos, upload definitivo, chatbot, alertas, clima personalizado ni categorías nuevas.

## PENDIENTE — detectado durante Fase 9 (no iniciar sin instrucción explícita)
- [x] Persistencia local de "Mi Cultivo" — resuelto en Fase 10A con `localStorage` (ver `39_PHASE_10A_MI_CULTIVO_PERSISTENCE.md`). Persistencia real entre dispositivos/con cuenta sigue bloqueada por la misma decisión de infraestructura de auth pendiente desde fases tempranas (ver sección BLOCKED).
- [ ] Upload y almacenamiento real de fotos para Mi Cultivo (privado por defecto, según `10_PRIVACY.md`) — no iniciado, ni siquiera como vista previa local.

## DONE (Fase 8C.1)
- [x] Fase 8C.1 — Home/Atlas separación real + grid completo + imagen de "De semilla al frasco": documentado en `MASTER_PACKAGE/37_PHASE_8C1_HOME_ATLAS_GRID_FIX.md`. La Home quedó reducida a pantalla de entrada (identidad, mapa, selección de provincia, dos accesos) — se quitaron de `page.js` la navegación superior placeholder, el Panel Ambiental, el grid de categorías y `HomeSecondaryAccess` (componentes conservados, solo dejaron de renderizarse en Home). Los botones del hero pasan a ser "Ingresar como usuario" (→ `/mi-cultivo`) e "Ingresar libremente" (→ `/atlas`). `/atlas/page.js` reemplazó su lista en filas por el grid cuadrado real de `CategoryShowcase` (nueva prop `showHeading`), mostrando las 8 tarjetas completas (7 categorías + "De semilla al frasco", ahora primera en el orden). Se reemplazó el rail de puntos de la tarjeta "De semilla al frasco" por una ilustración interna (SVG) de evolución de la planta, semilla a frasco. Se revisaron las 7 imágenes de categoría y se reemplazó únicamente "Luz y clima" (el diagrama de fitocromo, ilegible al recortarse a cuadrado) por una fotografía real nueva (`Plant_under_a_phytolamp.jpg`, Alla Varta, CC BY 4.0, verificada por lectura directa) — el diagrama original no se eliminó, sigue en uso como imagen de la entrada "luz-y-fotoperiodo" mediante un asset de categoría separado y nuevo (`asset-luz-clima-cover`). De paso se corrigió un overflow de footer en mobile (`.footer-links` sin `flex-wrap`) detectado al probar la nueva Home. `npm run build` limpio. Playwright en 1440×900/390×844: Home sin grid/panel/tracker/nav, ambos accesos navegando correctamente, Atlas con 8 tarjetas 1:1 (tracker primera), sin overflow, sin errores de consola. Regresión de `/api/geo`, `/api/climate`, `/mi-cultivo`, `/creditos` y las páginas de "Luz y clima" confirmada. No se implementó chatbot, auth real, base de datos ni sistema de Mi Cultivo.

## DONE (Fase 8C)
- [x] Fase 8C — Cuadrado "De semilla al frasco": documentado en `MASTER_PACKAGE/36_PHASE_8C_SEED_TO_JAR_CARD.md`. Se agregó una nueva tarjeta cuadrada (`TrackerShowcaseCard.js`) al grid del Atlas, visualmente distinta de las categorías botánicas (fondo verde oscuro, insignia "Nuevo", lista de 7 etapas con marcadores en vez de foto — composición interna con CSS, sin IA ni assets nuevos), sin agregarla como categoría editorial (se mantiene la separación entre contenido enciclopédico investigado y funcionalidad de producto futura). Lleva a una página nueva mínima `/mi-cultivo` que explica el concepto (las 7 etapas, el flujo con/sin cuenta, un placeholder reservado para fotos) sin implementar nada real todavía — ningún formulario, ningún upload, ningún login, todo marcado explícitamente como "todavía no disponible" con el mismo tono honesto que ya usa el Panel Ambiental. Se conectó el link "Mi Cultivo" del header (antes `href="#"` muerto) a `/mi-cultivo`. Categorías existentes sin cambios. `npm run build` limpio, nueva ruta `/mi-cultivo` generada. Playwright en 1440×900/390×844: tarjeta nueva con ratio 1:1, categorías existentes intactas, navegación real a `/mi-cultivo` confirmada, 7 etapas presentes, sin errores de consola, sin overflow. Regresión de `/api/geo`, `/api/climate`, `/atlas` confirmada. No se implementó autenticación, base de datos, upload, historial ni chatbot. No se tocó el mapa. No se inicia otra fase automáticamente.

## DONE (Fase 8B.1)
- [x] Fase 8B.1 — Hover del mapa + separación Home/Atlas: documentado en `MASTER_PACKAGE/35_PHASE_8B1_MAP_HOVER_AND_HOME_ATLAS_SPLIT.md`. (1) `.map-province.hovered` usaba `--sage-glass` (relleno casi transparente, pensado para veladuras sobre fondos claros) que se fundía con el fondo verde oscuro del mapa — corregido a `--sage-active` (sage sólido ya existente en la paleta), con los tres estados (reposo/hover/seleccionada) verificados como colores RGB distintos en el navegador real. (2) La Home mostraba el grid completo de 7 categorías inmediatamente debajo del Panel Ambiental, sin distinguirse de un recorrido completo del Atlas — `CategoryShowcase` ahora acepta `limit`/`description` opcionales (sin romper su comportamiento por defecto), y la Home muestra solo 3 categorías como adelanto más un botón "Ir al Atlas completo". `AtlasVisualCard` (tarjetas cuadradas de 8B) no se tocó. `npm run build` limpio. Playwright en 1440×900/390×844 confirmó colores de mapa distintos, navegación real Home→Atlas, `/atlas` intacto (7 filas), sin overflow de los componentes tocados, sin errores de consola. No se inició 8C, chatbot ni "Historia de la planta".

## DONE (Fase 8B)
- [x] Fase 8B — Grid de categorías en formato cuadrado: documentado en `MASTER_PACKAGE/34_PHASE_8B_CATEGORY_GRID.md`. Las tarjetas de `CategoryShowcase`/`AtlasVisualCard` eran rectangulares y tenían dos bugs de layout: (1) el contenedor aplicaba dos clases de grid en conflicto (`category-grid` de 5 columnas, resto de un diseño anterior no usado en ningún otro lado, vs. `atlas-grid` de 3 columnas), ganando la de 5 columnas y apretando el contenido; (2) `.atlas-card-image` no tenía ninguna regla CSS, causando que la imagen se renderizara a tamaño intrínseco — la causa raíz confirmada del overflow horizontal detectado en Fase 8A. Se rediseñaron las tarjetas a `aspect-ratio: 1/1` real, con la imagen como fondo completo del cuadrado y el título/descripción/CTA superpuestos en un panel inferior con degradado (máx. 2 líneas de descripción, sin forzar crecimiento). Se corrigió además un título sin estilo (`.atlas-card-title-link` se veía como link azul por defecto del navegador). Caso especial resuelto sin tocar ningún asset: la única categoría con ilustración SVG interna ("Marco legal") usa `object-fit: contain` en vez de `cover` para no cortar el texto dibujado dentro del archivo. Responsive: 3 columnas desktop, 2 tablet, 1 mobile, proporción cuadrada mantenida en las tres. `npm run build` limpio. Playwright en 1440×900/768×1024/390×844: 7 tarjetas con ratio 1:1 exacto, sin overflow del componente, navegación funcional, sin errores de consola. Regresión de `/api/geo`, `/api/climate`, `/atlas` confirmada; mapa de Fase 8A no tocado. No se creó el panel "Historia de la planta" ni se inició 8C.

## PENDIENTE — detectado durante Fase 8B (no iniciar sin instrucción explícita)
- [ ] El overflow horizontal en mobile ya NO lo causa `.atlas-card-image` (corregido en 8B) — la causa actual identificada es `.secondary-access-card` en `HomeSecondaryAccess.js` (fila "Biblioteca visual / Calendario / Comparador / Newsletter"). Fuera del alcance de 8B; pendiente para una futura fase que sí incluya ese componente.
- [ ] "Marco legal" muestra "Marco legal" dos veces (ilustración + título de tarjeta) y "CONTEXTO CONTEXTO" duplicado en el meta (tag y type con el mismo valor en `editorialData.js`) — ya no se superponen/cortan, pero es una redundancia de contenido preexistente, no un bug de layout; fuera de alcance de una tarea de CSS/grid.

## DONE (Fase 8A)
- [x] Fase 8A — Corrección del mapa geográfico de Argentina: documentado en `MASTER_PACKAGE/33_PHASE_8A_MAP_CORRECTION.md`. Se reemplazó el mapa anterior (un blob SVG dibujado a mano, sin relación real con la geografía, con solo 9 de 24 provincias siquiera seleccionables) por la geometría oficial de las 23 provincias + CABA, obtenida de georef-ar-api (`apis.datos.gob.ar`, fuente declarada IGN en cada feature). Script reproducible `scripts/build-argentina-provinces.py` (sin dependencias externas): filtra Tierra del Fuego a Isla Grande + Isla de los Estados (excluye Antártida/Malvinas/Georgias/Sandwich, fuera de escala continental), proyecta con cónica de Albers ajustada a Argentina, simplifica con Douglas-Peucker (2,5km de tolerancia) y genera `src/app/lib/geo/argentinaProvinces.js`. `GeoSelector.js` reescrito para renderizar las 24 geometrías reales con hover/selección/teclado; CABA resuelta con un hitbox invisible ampliado sobre su centroide real (su polígono real es minúsculo a esta escala, no se deformó). IDs internos y flujo de selección de zona sin cambios. `npm run build` limpio. Playwright en 1440×900/1280×800/768×1024/390×844: mapa visible con 24 provincias en las 4 resoluciones, sin overflow del componente, selección por click y por teclado funcional, CABA seleccionable. Regresión confirmada: `/api/geo`, `/api/climate`, `/atlas` sin romperse. No se tocó contenido editorial, assets, clima ni ninguna otra fase.

## PENDIENTE — detectado durante Fase 8A (no iniciar sin instrucción explícita)
- [ ] Overflow horizontal en mobile (390px) causado por `.atlas-card-image` dentro de `CategoryShowcase` (grilla de categorías del Atlas) — confirmado que no lo causa el mapa ni `GeoSelector`. Pendiente para una futura revisión de `CategoryShowcase`/Fase 3-4, fuera del alcance de 8A.

## DONE (Fase 7C)
- [x] Fase 7C — Cierre editorial + recuperación de PDFs: documentado en `MASTER_PACKAGE/32_PHASE_7C_CLOSURE.md`. Se revisaron las 7 entradas del Atlas como conjunto coherente (IDs, categorías, `relatedEntryIds`, tags, `editorialStatus`, `lastReviewed`, `seoTitle`/`seoDescription`, fuentes, assets, estructura) — no se encontró ninguna inconsistencia real que corregir, se documentó la revisión. Recuperación de PDFs: se instaló PyMuPDF (Python, libre/open-source, sin compilación) en un entorno virtual aislado de sesión, en vez de reintentar `poppler`/`pdftotext` (cuya instalación había quedado incompleta en 7B2). Con esto se recuperaron y verificaron 3 de las 4 fuentes que quedaron `PARTIAL` en 7B2: UNLPam+INTA Anguil ("Guía evaluación visual del suelo"), UNNE ("Fitocromos y desarrollo vegetal") y UNCUYO ("Guía N°3 Crecimiento") — las 3 pasan a `VERIFIED` en `sources.js`/`19_SOURCE_REGISTRY.md`. La cuarta (INTA "Relación suelo–planta–agua") sigue `PARTIAL`: la descarga falló por reset de conexión TLS al dominio del repositorio institucional, mismo patrón de bloqueo de dominios INTA ya documentado en `21_GEO_CLIMATE_RESEARCH.md`. Ninguna entrada requirió corrección de contenido — el texto recuperado confirmó lo ya publicado sin necesidad de agregar ni cambiar nada. `npm run build` limpio. Verificación Playwright en 1440×900/390×844 sobre las 7 entradas + Home + `/atlas` + `/creditos` (20 combinaciones): status 200, un solo `h1`, cero imágenes rotas, cero errores de consola, cero fugas de contenido interno (verificado sobre texto visible, no sobre HTML/JS interno de RSC). Regresión confirmada: `/api/geo`, `/api/climate`, mapa, GeoSelector, Panel Ambiental sin tocar. No se creó ninguna funcionalidad nueva. No se inicia auditoría final ni ninguna fase nueva.

## PENDIENTE — detectado durante Fase 7C (no iniciar sin instrucción explícita)
- [ ] 1 fuente sigue con verificación parcial: INTA "Relación suelo-planta-agua" — bloqueada por reset de conexión TLS a `repositorio.inta.gob.ar`, no por ser PDF escaneado. Reintentar en el futuro desde una red distinta; no es necesario reinstalar ninguna herramienta (PyMuPDF ya demostró funcionar para este tipo de PDF). No se cita para ninguna afirmación específica, solo como respaldo convergente.
- [ ] La auditoría integral del proyecto (arquitectura, decisiones de infraestructura BLOCKED, matriz extendida de capturas) queda explícitamente para VSC — no iniciarla en Claude Code.

## DONE (Fase 7B2)
- [x] Fase 7B2 — Escalado editorial del Atlas: documentado en `MASTER_PACKAGE/31_PHASE_7B2_EDITORIAL_SCALE.md`. Se migraron las 6 entradas restantes (`sustrato-y-drenaje`, `luz-y-fotoperiodo`, `lectura-de-senales`, `cultivo-en-secuencia`, `cosecha-y-maduracion`, `marco-editorial`) al modelo editorial completo, con investigación real priorizando fuentes oficiales/académicas argentinas (INTA ×2, UNLPam+INTA Anguil, UNNE, UNCUYO) y un paper científico internacional revisado por pares y verificado en texto completo (Hesami et al. 2023, *Plants*) usado como fuente principal de 3 entradas. `marco-editorial` reutilizó las fuentes legales ya verificadas en Fase 7A sin investigación nueva. Se formalizó el vocabulario de `signals`/`commonMistakes` en inglés (`EXPECTED`/`ATTENTION`/`AMBIGUOUS`, `OBSERVATION`/`INTERPRETATION`/`CONTEXT`/`OTHER`) con traducción a español en la UI — aplicado también a `germinacion` (piloto) por ser un ajuste estructural transversal, sin cambiar su contenido en español. `marco-editorial` queda con `signals`/`environmentContext` en `null` por diseño (no le corresponde naturaleza botánica-observacional), sin rellenar artificialmente. Todas las 6 entradas quedan `editorialStatus: 'PUBLISHED'` con `lastReviewed: '2026-09-10'`. `npm run build` limpio. Verificación con Playwright en 1440×900/390×844 sobre las 6 rutas nuevas + `germinacion` + `/atlas` + Home + `/creditos` (20 combinaciones): status 200, cero errores de consola, cero imágenes rotas, un solo `h1` por página, cero texto técnico expuesto. Regresión confirmada: `/api/geo`, `/api/climate`, mapa, GeoSelector y Panel Ambiental sin tocar. No se buscaron ni cambiaron assets visuales. No se creó ninguna funcionalidad nueva (CMS, DB, login, newsletter, alertas, calendario regional). No se ejecuta 7C automáticamente.

## PENDIENTE — detectado durante Fase 7B2 (no iniciar sin instrucción explícita)
- [x] 4 fuentes quedaron con verificación parcial en 7B2 — **resuelto en Fase 7C para 3 de 4** (ver sección "DONE (Fase 7C)" y "PENDIENTE — detectado durante Fase 7C" arriba): UNLPam/INTA Anguil, UNNE Fitocromos y UNCUYO Crecimiento pasaron a `VERIFIED`. INTA "Relación suelo-planta-agua" sigue `PARTIAL` (bloqueo de red, no de herramienta).
- [ ] Ninguna de las 7 entradas tiene todavía contenido regional/provincial específico (todas mencionan la diversidad climática argentina en general, sin fuente propia por provincia) — pendiente para una fase futura de contenido geográfico.

## PENDIENTE — detectado durante el piloto Germinación (Fase 7B1, no iniciar sin instrucción explícita)
- [ ] Replicar el patrón de `germinacion` en las otras 6 entradas (`sustrato-y-drenaje`, `luz-y-fotoperiodo`, `lectura-de-senales`, `cultivo-en-secuencia`, `cosecha-y-maduracion`, `marco-editorial`): investigar fuentes reales con prioridad argentina/académica, escribir `sections`/`observations`/`signals`/`commonMistakes`/`environmentContext` reales, revisar sus `alt` de asset. Fuera de alcance de 7B1 por regla explícita — es la Fase 7B2 recomendada, no iniciada automáticamente.
- [ ] Decidir si fijar formalmente la forma `{level, description}` para `signals` y `{type, description}` para `commonMistakes` como estándar del modelo editorial antes de escalar a más entradas (recomendado en `30_GERMINACION_PILOT.md` §24, para evitar que cada entrada futura use una forma distinta).
- [ ] Verificar por lectura directa (fuera de esta sesión/entorno) las 2 guías universitarias (UNNE, UNPSJB) y confirmar autoría/revista real de la publicación 2023 sobre latencia de semilla de cannabis — las 3 quedaron con verificación parcial en `19_SOURCE_REGISTRY.md`/`sources.js`.
- [ ] Revisar si existe una página INTA real y vigente sobre germinación/poder germinativo (la URL específica probada en esta ronda ya no resuelve a contenido propio, redirige a la home de INTA).

## DONE
- [x] Fase 7B1 — Piloto editorial completo: Germinación. Documentado en `MASTER_PACKAGE/30_GERMINACION_PILOT.md`. Se investigaron fuentes reales con prioridad argentina/académica (INASE, Cátedra de Fisiología Vegetal UNNE, Cátedra de Fisiología General UNPSJB, y dos publicaciones científicas internacionales sobre germinación de *Cannabis sativa* — Latif et al. 2025 en *Scientific Reports*, y un estudio 2023 sobre latencia de semilla con autoría/revista parcialmente verificada). Se reescribió por completo la entrada `germinacion` (único entry tocado, ninguna de las otras 6 se modificó): resumen corregido (ya no atribuye la luz como factor de germinación), intro reescrita, 5 `sections` reales (¿Qué es la germinación?, Qué está ocurriendo, Primeras señales, Condiciones ambientales, Lectura de la plántula — incluye la explicación de germinación epigea específica de *Cannabis sativa*, que valida el uso del asset de diagrama epigeo/hipogeo ya elegido en Fase 6), `observations` (separación explícita observación/interpretación), `signals` estructuradas en 3 niveles semánticos (`ESPERABLE`/`ATENCIÓN`/`AMBIGUA`), `commonMistakes` tipados (interpretación/observación/contextualización), y `environmentContext` (ambiente protegido vs. exterior, diversidad climática argentina mencionada en general sin inventar diferencias provinciales sin fuente). `lastReviewed: '2026-09-10'`, `editorialStatus: 'PUBLISHED'` con matices de verificación documentados con transparencia. Se revisó el `alt` del asset de germinación (antes mezclaba descripción y atribución, ahora es puramente descriptivo). Se detectó y corrigió durante la implementación una fuga real: una nota de verificación interna había quedado en un campo (`authorOrInstitution`) que la UI muestra públicamente en "Fuentes" — corregida moviéndola a `notes` (nunca renderizado) tras detectarla en una captura de pantalla de verificación. Se detectó y resolvió un `MODEL_GAP` de Fase 7A: `observations`/`signals`/`commonMistakes`/`environmentContext` como `string | null` no alcanzaban para representar contenido real — se ampliaron a arreglos estructurados (documentado en `28_EDITORIAL_CONTENT_MODEL.md`), cambio aditivo que no afecta a las otras 6 entradas (siguen en `null`). `npm run build` exit 0 antes y después de la corrección. Verificación con Playwright en 1440×900/1280×800/390×844 sobre Home/`/atlas`/`/atlas/fundamentos`/`/atlas/fundamentos/germinacion`: status 200, cero errores de consola, cero imágenes rotas, cero texto técnico, exactamente un `<h1>` por página — confirmado en dos rondas (antes y después de la corrección de la fuga). Regresión confirmada: `/api/geo`, `/api/climate`, mapa, `GeoSelector` y Panel Ambiental sin tocar. No hay tests ni lint configurados en el proyecto (verificado, no omitido). No se usó Pollinations ni generación de imágenes, no se buscó ni reemplazó ningún asset visual, no se migraron las otras 6 entradas. No se inicia Fase 7B2 automáticamente.
- [x] Fase 7A — Normalización del modelo editorial: documentada en `MASTER_PACKAGE/28_EDITORIAL_CONTENT_MODEL.md` y `MASTER_PACKAGE/29_PHASE_7A_IMPLEMENTATION.md`. Se construyó una capa editorial estructurada (`src/app/lib/editorial/{editorialData,sources,assets,tags,registry}.js`) separando contenido, fuentes, assets, relaciones, tags, estado editorial y metadata — antes mezclados en un único catálogo plano (`atlasData.js`). Se migraron las 7 categorías y 7 entradas existentes sin escribir contenido nuevo (`intro` preserva el texto original verbatim). `related` (slugs de categoría) se migró a `relatedEntryIds` (IDs de entrada real) sin ambigüedad, porque cada categoría tenía exactamente 1 entrada publicada. Se estructuraron 11 fuentes reales (5 legales + 6 visuales, todas ya existentes en `19_SOURCE_REGISTRY.md`, ninguna investigada de nuevo) y se mapearon los 8 assets reales de Fase 6 sin buscar ni reemplazar ningún archivo. Las 7 entradas mantienen `editorialStatus: 'PUBLISHED'` — decisión explícita de no degradarlas a `REVIEW` solo por introducir el campo formal (D12 en `16_DECISIONS.md`). `atlasData.js` pasó de fuente de verdad a adaptador derivado (D11) — Home, `/atlas` y la página de categoría siguen consumiéndolo sin cambios; solo la página de entrada se reescribió para consumir el nuevo registry y mostrar "Fuentes" únicamente cuando existen (hoy solo en "Marco editorial y responsable", con 3 fuentes legales reales). Se creó `/creditos`, página pública que lista los 7 assets con licencia externa (imagen, licencia, crédito, enlace a la fuente) sin IDs internos ni JSON. Se corrigieron dos enlaces muertos (`href="#"`) — nav "Enciclopedia" → `/atlas`, footer "Créditos" → `/creditos` — cambio mínimo necesario para que `/creditos` fuera navegable. Se detectó y corrigió un bug propio durante la implementación: el cálculo de categoría de entradas relacionadas devolvía siempre la categoría actual en vez de la real; corregido con `getCategoryById()` en el registry y verificado visualmente. `npm run build` exit 0 en cada punto de verificación. Verificación con Playwright (Chromium headless) en 1440×900, 1280×800 y 390×844 sobre Home, `/atlas`, `/atlas/fundamentos`, `/atlas/fundamentos/germinacion`, `/atlas/marco-legal/marco-editorial` y `/creditos` (18 combinaciones): status 200, cero errores de consola, cero imágenes rotas, cero `NO_IMPLEMENTADO`/"pollinations"/`undefined` en el texto renderizado. Regresión confirmada explícitamente: `/api/geo` y `/api/climate` responden igual, mapa/`GeoSelector`/Panel Ambiental no fueron tocados en ningún archivo de esta fase. No existen tests automatizados en el proyecto (sin script `test` en `package.json`, sin archivos `*.test.js`) — no se ejecutó ninguno porque no hay ninguno; tampoco existe script `lint` separado (el build corre TypeScript como parte de `next build`). No se instaló ninguna dependencia nueva. No se usó Pollinations ni ninguna IA generadora de imágenes, no se buscaron imágenes nuevas, no se escribió contenido editorial masivo. Pendiente explícito para Fase 7B: escribir `sections`/`observations`/`signals`/`commonMistakes`/`environmentContext` reales, mapear fuentes de contenido para las 6 entradas que hoy tienen `sourceIds: []`, definir `lastReviewed` cuando exista revisión real, y ampliar entradas por categoría (el modelo ya lo soporta, hoy es 1:1). No se inicia Fase 7B automáticamente.
- [x] Fase 6 — Curaduría visual real + fuentes abiertas (dirección RELIEVE): documentada en `MASTER_PACKAGE/27_VISUAL_SOURCE_AND_CURATORIAL_POLICY.md`, `MASTER_PACKAGE/ASSET_REGISTRY.md` y ampliación de `MASTER_PACKAGE/19_SOURCE_REGISTRY.md`. Se auditaron los 7 assets de Fase 4/6B y se investigó en Wikimedia Commons (fuentes reales, sin IA, sin Pollinations) con verificación de licencia por lectura directa de cada página de archivo. Resultado: **6 de 7 categorías REEMPLAZADAS** con material real y licenciado — Fundamentos (lámina botánica histórica de *Cannabis sativa*, Köhler 1887, dominio público, más un diagrama de germinación CC BY-SA 3.0 específico para esa entrada), Suelo y agua (fotografía documental de perfil de suelo, CC BY 4.0), Luz y clima (diagrama real del ciclo del fitocromo, CC BY-SA 4.0), Sanidad (fotografía macro real de arañuela roja/*Tetranychus urticae*, CC BY 2.0 — **resuelve el `PENDIENTE` que la Fase 6B había dejado sin cerrar**, confirmando que la sanidad necesitaba documentación real y no generación), Cultivo (fotografía documental de un cultivo exterior de cáñamo industrial en Francia, GFDL/CC BY-SA — se prefirió precisión de especie sobre "argentinidad" frente a un candidato argentino de vid en Cafayate, descartado por irrelevancia temática) y Cosecha (fotografía documental de inflorescencias secándose, CC BY 2.0, aceptada con una reserva estética documentada por su tono más cálido/atractivo que el resto de la serie). **Marco legal queda `CONSERVAR`** con su SVG interno original — no se encontró ninguna fotografía o ilustración real relevante y no arbitraria para un concepto de contexto legal/institucional. Se descartaron candidatos por baja calidad (`Soil_horizons.JPG`), irrelevancia (`Harvest.jpg`, cosecha de sorgo) o imprecisión temática (viñedo de Cafayate para Cultivo). El mapa funcional de `GeoSelector` no se tocó; no se generó ni se usó ninguna cartografía nueva. Los 6 assets de Pollinations de la Fase 6B quedan en disco sin usar (no borrados, ver `ASSET_REGISTRY.md`) y el documento `27B_POLLINATIONS_VISUAL_EXPERIMENT.md` permanece intacto como experimento cerrado. Verificación con Playwright (Chromium headless) en 1440×900, 1280×800 y 390×844 sobre Home, `/atlas`, `/atlas/fundamentos`, `/atlas/fundamentos/germinacion`, `/atlas/sanidad` y `/atlas/marco-legal`: cero `NO_IMPLEMENTADO`/jerga técnica/menciones de "pollinations", cero imágenes rotas en las 18 combinaciones. `npm run build` exit 0. No existen tests automatizados en el proyecto (no hay script `test` en `package.json` ni archivos `*.test.js`/`*.spec.js`) — no se ejecutó ninguno porque no hay ninguno que ejecutar; el build y la verificación de navegador real cumplen el rol de suite de verificación de esta fase. Regresión confirmada: `/api/geo` y `/api/climate` responden igual que antes. No se instaló ninguna dependencia nueva (se usó `curl`, `python3` stdlib para URL-encoding, `sips` — herramienta nativa de macOS — para optimizar tamaño de imágenes, y `playwright`, ya instalado). Pendientes explícitos para la próxima ronda: página de atribución/créditos para las 6 imágenes con licencia CC/GFDL (todavía no existe en el sitio), mejora de recorte/composición de la miniatura de "Luz y clima", revisar la reserva estética de "Cosecha" si aparece una alternativa más sobria, ampliar la biblioteca visual más allá de estas 7 categorías (fotografías secundarias, nuevas entradas de contenido), y seguir buscando material fotográfico real específicamente argentino y on-topic (no encontrado dentro del alcance de esta ronda).
- [x] Fase 6B — Experimento visual 100% Pollinations (dirección RELIEVE): documentado en `MASTER_PACKAGE/27B_POLLINATIONS_VISUAL_EXPERIMENT.md`. Se generaron y evaluaron 17 imágenes (máximo 3 intentos por asset) para los 7 assets de categoría de la Fase 4 más dos pruebas experimentales (mapa y vista aérea), usando exclusivamente `image.pollinations.ai` (sin API key, sin costo, sin búsqueda web, sin bancos de imágenes). Resultado: **4 de 7 categorías integradas** en `atlasData.js`/`AtlasVisualCard` (Fundamentos 15/20, Suelo y agua 19/20, Luz y clima 17/20, Cultivo 17/20 — reemplazan el SVG de Fase 4 en categoría y en su entrada de contenido asociada); **2 aceptadas pero retenidas sin integrar** por defectos puntuales (Cosecha 14/20 con texto falso ilegible sin limpiar; Marco legal 15/20 con registro visual — ícono plano — inconsistente con el resto de la serie); **Sanidad queda PENDIENTE** (3 intentos, ninguno superó 11/20 — el modelo no logra representar un concepto de diagnóstico/comparación sin caer en simetría decorativa, límite reproducible documentado, no se fabricó una imagen mala para completar la colección; sigue con el SVG de Fase 4). El mapa generado con Pollinations falló como cartografía en sus dos intentos (`FAILED_GENERATIVE_MAP_TEST`, formas incorrectas y texto ilegible inventado) y **no se usó** para navegación ni se integró en ningún componente — el mapa funcional de `GeoSelector` permanece intacto. La prueba de vista aérea se documentó como `GENERATED_EDITORIAL_IMAGE` experimental, sin uso en producción. Verificación real con Playwright (Chromium headless) en 1440×900, 1280×800 y 390×844 sobre Home, `/atlas`, `/atlas/fundamentos` y `/atlas/fundamentos/germinacion`: cero apariciones de `NO_IMPLEMENTADO`/jerga técnica, cero imágenes rotas en las 12 combinaciones. `npm run build` exit 0. Regresión confirmada: `/api/geo` y `/api/climate` responden igual que antes, mapa/GeoSelector/Panel Ambiental sin cambios funcionales. No se instaló ninguna dependencia nueva (Playwright ya estaba instalado de una fase previa). Licencia/términos de uso de las imágenes de Pollinations quedan `TODO RESEARCH` explícito antes de cualquier lanzamiento real (esta ronda prohibía research web). Próxima tarea concreta: limpiar el artefacto de texto falso del candidato de Cosecha y restilizar el candidato de Marco legal antes de integrarlos; evaluar Sanidad en la futura Fase 6 de curaduría real en vez de seguir generando.
- [x] Loop 2.5 — Verificación visual real + corrección del tablero de Home RELIEVE: captura real de navegador en desktop 1440×900 y mobile 390×844, con SVG del mapa y selector visibles; flujo de selección de provincia verificado con Playwright en navegador real; instalación y uso de Playwright y @fontsource para la verificación visual; imports locales CSS de tipografía para Inter y Source Serif 4; evidencia en `screenshots/home-desktop-verify.png` y `screenshots/mobile-390-844.png` y `tmp-home-desktop-1440.png`; ausencia de `NO_IMPLEMENTADO` desde el HTML renderizado y ausencia de lenguaje técnico en la capa pública del mapa; comparación de servidor local y túnel: local responde con el contenido visual verificado; túnel permanece activo como proceso de Cloudflare y debe seguirse comparando al final de la iteración, sin declarar discrepancias irreconciliadas como si fueran implementaciones. Esta tarea se cierra con evidencia visual real, sin abrir fases nuevas ni agregar clima/regionalización.
- [x] Fase 4 — Primera colección visual real del atlas: se documenta `MASTER_PACKAGE/25_FIRST_ASSET_COLLECTION.md`, se integra la colección de 7 archivos SVG de categoría en la Home mediante `CategoryShowcase` y `AtlasVisualCard`, y se verifica visual y técnicamente con inspección de navegador real y build local. La colección se mantiene aún como activo editorial SVG interno, sin introducir clima, meteorología ni regionalización sobre el mapa ni en la capa de contenido.
- [x] Fase 5 — Atlas navegable + primera experiencia de contenido: se implementa el catálogo local reusable `atlasData.js`, rutas visuales y de contenido `/atlas`, `/atlas/[category]`, `/atlas/[category]/[entry]`, página `not-found`, breadcrumb y navegación editorial con entradas publicadas y relaciones. Se documenta `MASTER_PACKAGE/26_ATLAS_NAVIGATION_AND_CONTENT.md`. La arquitectura mantiene `Home` y `GeoSelector` intactos y no introduce meteorología, regionalización ni usuarios ya que la fase queda confinada a navegación editorial mínima.
- [x] Fase 3 — Sistema visual editorial del atlas: se implementa el patrón reusable `AtlasVisualCard` y `AtlasImage`/ficha editorial sin introducir una librería masiva de imágenes. Se añade el archivo `MASTER_PACKAGE/24_VISUAL_ASSET_SYSTEM.md` con la filosofía de diseño, estructura de assets y los roles de imagen/ilustración/mapa. En la Home, `CategoryShowcase` se transforma a una visualización de índice visual del atlas con ficha editorial y una estructura mime SVG mínima en `public/atlas/categories/`. Se evita crear un fichero profesional de fotos/illustración y se mantiene la separación geográfica, sin tocar `/api/geo`, clima, ni datos de meteorología.
- [x] Fase 1 — Rediseño visual de Home en dirección RELIEVE: página pública reducida a atlas editorial con selector geográfico/mapa como pieza protagonista, panel ambiental con estado más humano/editorial y vidriera de categorías / accesos secundarios; sin introducir `NO_IMPLEMENTADO` ni meteorología sobre el mapa ni datos ambientales falsos en la UI.
- [x] Fase 2 — Relieve cartográfico en Home: mapa SVG de Argentina integrado, selección visual de provincia, jerarquía cartográfica y actualización de la identidad visual del selector y de la composición editorial, manteniendo el contrato de `/api/geo` y el flujo geográfico `Argentina → provincia → contexto/zona aproximada`, sin abrir capas meteorológicas ni reducir el panel ambiental a un mock. Verificadas build y smoke HTTP; la verificación visual con Playwright no estaba disponible como dependencia local y se declina instalar una herramienta adicional para esta tarea, porque la implementación ya está respaldada por el build y la ruta servida localmente.
- [x] Primer artefacto estático de Foundation: `index.html`, `styles.css` y `app.js` con navegación pública, selector de ubicación y grid editorial inicial.
- [x] Elección cerrada de stack: `Next.js` App Router como frontend con soporte SSR/SSG y backend integrado en el mismo proyecto.
- [x] Opción de autenticación documentada como `Auth.js`/`NextAuth` y opción de storage documental como `Cloudflare R2` S3-compatible con buckets públicos y privados.
- [x] Rutas de autenticación de NextAuth creadas y verificadas con `npm run build` y `curl` sobre `/api/auth/session`.
- [x] Evidencia del `TODO RESEARCH` de INTA RIAN documentada: el dominio `agroregionesrian.inta.gob.ar` no resuelve en esta sesión (`curl -fsSL https://agroregionesrian.inta.gob.ar/inicio` → `Could not resolve host`).
- [x] Evidencia de investigación SMN: el atlas climático está publicado en repositorio institucional y existe, pero el HTML del repositorio no expone códigos Köppen ni una lectura completa del PDF; la clasificación climática oficial necesita revisión y extracción de la fuente antes de publicarla en el sistema de zonas.
- [x] `npm install` completado y `npm run build` verificado con `next build` exit 0.
- [x] Ronda de investigación 2026-09-09 (loop de cierre del gate agroclimático): documentada en `MASTER_PACKAGE/21_GEO_CLIMATE_RESEARCH.md`. Verificada por lectura directa una capa Köppen oficial (INDEC/ANIDA-IGN) — decisión D10 en `16_DECISIONS.md`. Regionalización agroecológica de INTA/RySA sigue sin lectura primaria (dominios inaccesibles desde este entorno); gate pasa de `CLAUDE_RESEARCH_REQUIRED` a `PARTIAL_RESEARCH`, no a `DONE`.
- [x] Estructura geográfica implementable en la app y separada de la clasificación climática todavía `PARTIAL_RESEARCH`: la aplicación ahora expone un endpoint de contrato `GET /api/geo` con la jerarquía `Argentina -> provincia -> departamento/partido -> zona editorial aproximada`, y el contrato deja explícito que no se inferencian datos climáticos ni regionalizaciones no verificadas.
- [x] Especificación funcional/UX de Home y recorrido principal (sin implementación): documentada en `MASTER_PACKAGE/22_HOME_EXPERIENCE.md`. Define qué ve un visitante al entrar, jerarquía visual de Home, rol exclusivamente geográfico del mapa (nunca meteorológico), el flujo `Argentina → Provincia → Departamento/Partido → Zona aproximada` (el departamento/partido queda como capa de datos, nunca como paso de navegación obligatorio para el usuario), comportamiento tras elegir ubicación, ubicación conceptual y 5 estados del futuro "Panel Ambiental" (sin proveedor ni datos reales todavía), la cadena `datos meteorológicos → condición relevante → contenido del Atlas` con separación estricta entre capas, diferencia entre visitante/usuario registrado/"Mi Cultivo", qué vive en Home vs páginas secundarias, mobile vs desktop, componentes conceptuales, decisiones pendientes y criterios de aceptación. Verificada coherencia contra `01_PRODUCT.md`, `02_UX.md`, `03_GEO.md`/D1, `04_CLIMATE.md`, `21_GEO_CLIMATE_RESEARCH.md` y `08_ARCHITECTURE.md` — sin contradicciones detectadas. No se tocó código, no se crearon componentes, no se instalaron dependencias.
- [x] Auditoría visual brutal de la Home real (código + estilos, sin implementación) y nueva dirección visual "RELIEVE": documentada en `MASTER_PACKAGE/23_HOME_VISUAL_REDIRECTION.md`. Inspección real de `page.js`, `GeoSelector.js`, `EnvironmentalPanel.js`, `CategoryShowcase.js`, `HomeSecondaryAccess.js` y `globals.css`, con cruce clase por clase entre JSX y CSS. Hallazgos críticos verificados en código: (1) la cadena técnica `NO_IMPLEMENTADO` se filtra al usuario en 7 lugares distintos de la misma pantalla, más otras fugas de jerga (`"Cargando contrato geográfico"`, `geographic_hierarchy` concatenado y mostrado crudo); (2) no existe ningún mapa real — el selector geográfico es un formulario de 3 inputs con autocompletado; (3) "Departamento/Partido" se expone como paso de navegación visible, contradiciendo `22_HOME_EXPERIENCE.md` §4; (4) las clases `home-intro`, `home-intro-copy`, `home-env`, `home-frame`, `home-frame-heading` y `category-section` no tienen ninguna regla CSS asociada — el layout de dos columnas del hero (`.hero`) nunca se aplica; (5) cero imágenes en todo el proyecto (`public/` vacío) pese a la ambición de biblioteca visual de `06_VISUAL.md`; (6) la fuente serif declarada (`Iowan Old Style`) es exclusiva de Apple y no se carga ninguna fuente real, cayendo a Times New Roman para la mayoría de usuarios; (7) "Clima" figura como pilar de marca/nav principal, en tensión con la regla de no parecer una app de clima. Se intentó ver la app renderizada (servidor de dev ya estaba corriendo en :3000, respondió 200) pero no hay Playwright/navegador instalable en este entorno sin violar la regla de no instalar nada — limitación declarada explícitamente en el documento; auditoría basada en código/CSS/estructura real, no en captura de pantalla. No se encontró ninguna decisión registrada en `16_DECISIONS.md` (D1-D10) que deba reabrirse — los problemas son de implementación, no de las decisiones de producto ya tomadas; no se marcó ningún `DECISION_REVIEW_REQUIRED`. No se tocó código, no se instaló nada, no se hizo research web. Próxima tarea concreta: ejecutar Fase 1 de `23_HOME_VISUAL_REDIRECTION.md` §26 (eliminar lenguaje técnico visible, quitar campo de Departamento/Partido, corregir el desalineamiento CSS/JSX, quitar contador de estadísticas/franja de features/artículos ficticios) — no iniciada en este loop.

